import styled from '@emotion/styled'

const UserRowActionsBlock = styled('div')({
  display: 'flex',
  justifyContent: 'flex-end',
  fontSize: 0,
  textAlign: 'right'
})

export default UserRowActionsBlock
