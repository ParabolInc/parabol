import styled from 'react-emotion';

const PlainButton = styled('button')({
  appearance: 'none',
  background: 'inherit',
  border: 0,
  borderRadius: 0,
  color: 'inherit',
  cursor: 'pointer',
  fontSize: 'inherit',
  margin: 0,
  padding: 0
});

export default PlainButton;
