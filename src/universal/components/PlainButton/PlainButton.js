import styled from 'react-emotion';

const PlainButton = styled('button')({
  appearance: 'none',
  background: 'inherit',
  border: 0,
  borderRadius: 0,
  cursor: 'pointer',
  margin: 0,
  padding: 0
});

export default PlainButton;
