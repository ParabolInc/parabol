import styled from 'react-emotion'

const TeamManagementModalBoundary = styled('div')({
  background: 'white',
  borderRadius: '4px',
  display: 'flex',
  flexDirection: 'column',
  maxWidth: '100%',
  width: 320
})

export default TeamManagementModalBoundary
