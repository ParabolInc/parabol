
// export default function fullySubscribed(subs) {
//   if (subs.length < 3) return false;
//   const reqSubs = ['team', 'teamMembers', 'presence'];
//   for (let i = 0; i < reqSubs.length; i++) {
//     const sub = subs[i];
//     if (!subs.includes(sub)) return false;
//   }
//   return true;
// }
