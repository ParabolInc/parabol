import ui from 'universal/styles/ui';

const cardRootStyles = {
  backgroundColor: '#fff',
  border: 0,
  borderRadius: ui.cardBorderRadius,
  borderTop: '1px solid',
  boxShadow: ui.cardBoxShadow[0],
  position: 'relative',
  width: '100%'
};

export default cardRootStyles;
