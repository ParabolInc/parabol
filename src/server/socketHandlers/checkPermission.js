// export default function checkPermission(authorizationArray, meetingId) {
//
//   return Array.isArray(authorizationArray) && Boolean(authorizationArray.find(role => role.meetingId === meetingId));
// };
