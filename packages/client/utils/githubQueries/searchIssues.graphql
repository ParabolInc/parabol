query searchIssues($queryString: String!) {
  search(query: $queryString, type: ISSUE, first: 10) {
    edges {
      node {
        ... on Issue {
          id
          title
          url
          repository {
            nameWithOwner
          }
        }
      }
    }
  }
}
