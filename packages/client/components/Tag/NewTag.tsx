import styled from '@emotion/styled'
import BaseTag from './BaseTag'
import {PALETTE} from '../../styles/paletteV2'

const NewTag = styled(BaseTag)({
  backgroundColor: PALETTE.BACKGROUND_PINK,
  color: '#FFFFFF'
})

export default NewTag
