import styled from '@emotion/styled'

const BaseTag = styled('div')({
  borderRadius: '4em',
  fontSize: 11,
  fontWeight: 600,
  height: 16,
  lineHeight: '16px',
  marginLeft: 8,
  padding: '0 8px',
  textAlign: 'center',
  userSelect: 'none'
})

export default BaseTag
