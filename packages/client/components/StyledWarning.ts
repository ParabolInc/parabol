import styled from '@emotion/styled'
import {PALETTE} from '../styles/paletteV2'

const StyledWarning = styled('div')({
  color: PALETTE.PROMPT_ORANGE,
  fontWeight: 600,
  textAlign: 'center'
})

export default StyledWarning
