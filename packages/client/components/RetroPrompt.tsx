import styled from '@emotion/styled'

const RetroPrompt = styled('div')({
  alignItems: 'center',
  display: 'flex',
  fontSize: 18,
  fontWeight: 600,
  lineHeight: '24px'
})

export default RetroPrompt
