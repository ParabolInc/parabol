query getProjectIssues($fullPath: ID!, $first: Int!) {
  project(fullPath: $fullPath) {
    id
    fullPath
    issues(first: $first) {
      pageInfo {
        hasNextPage
      }
      edges {
        node {
          __typename
          id
          iid
          descriptionHtml
          webPath
          updatedAt
          title
        }
      }
    }
  }
}
