mutation createLabel(
  $name: String!
  $teamId: String!
  $color: String
) {
  issueLabelCreate(
    input: {
      name: $name
      teamId: $teamId
      color: $color
    }
  ) {
    success
    issueLabel {
      id
      name
      color
    }
  }
}