query getProjects($first: Int, $after: String, $ids: [ID!]) {
  projects(first: $first, after: $after, filter: {id: {in: $ids}}) {
    edges {
      node {
        __typename
        id
        name
        teams {
          nodes {
            id
            name
            key
          }
        }
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
