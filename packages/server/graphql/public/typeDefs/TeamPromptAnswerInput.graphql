"""
One answer to one prompt of a templated stand-up
"""
input TeamPromptAnswerInput {
  """
  The id of the prompt being answered. Must be one of the meeting's prompts
  """
  promptId: ID!

  """
  The stringified TipTap document of the answer. An empty document removes the answer
  """
  content: String!
}
