"A GitLab issue linked to a Parabol task"
type _xGitLabIssue implements TaskIntegration

# Declared so GitLabIntegration.projects can name it: the public SDL is merged before the GitLab schema is nested, and mergeSchemas rejects an unknown type. The stitcher supplies every field
"A GitLab project, exposed for scoping and issue creation"
type _xGitLabProject

extend type _xGitLabIssue {
  service: IntegrationProviderServiceEnum!
}
