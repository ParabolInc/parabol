"A GitLab issue linked to a Parabol task"
type _xGitLabIssue implements TaskIntegration

"A GitLab project available for integration"
type _xGitLabProject implements RepoIntegration

extend type _xGitLabProject {
  service: IntegrationProviderServiceEnum!
  """
  The id createTaskIntegration expects for this repo. Differs from id where id is the vendor's node id
  """
  integrationRepoId: ID!
}

extend type _xGitLabIssue {
  service: IntegrationProviderServiceEnum!
}
