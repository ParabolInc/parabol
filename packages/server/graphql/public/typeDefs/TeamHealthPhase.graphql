"""
The meeting phase where team members answer health check questions
"""
type TeamHealthPhase implements NewMeetingPhase {
  "Unique identifier"
  id: ID!
  meetingId: ID!
  teamId: ID!

  """
  The type of phase
  """
  phaseType: NewMeetingPhaseTypeEnum!
  stages: [TeamHealthStage!]!
}
