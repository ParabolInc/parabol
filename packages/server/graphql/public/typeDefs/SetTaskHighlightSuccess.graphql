"""
Success result for setTaskHighlight mutation
"""
type SetTaskHighlightSuccess {
  "ID of the meeting where the task is highlighted"
  meetingId: ID!

  "ID of the task whose highlight changed"
  taskId: ID!

  "The task whose highlight changed"
  task: Task!
}
