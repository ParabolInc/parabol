"""
Success result for setMeetingMusic mutation
"""
type SetMeetingMusicSuccess {
  "The meeting whose music setting was updated"
  meetingId: ID!
  "URL of the audio track, or null if music was disabled"
  trackSrc: String
  "Whether music is currently playing"
  isPlaying: Boolean!
}
