type SearchMatchScore {
  # RRF of vectorRank and keywordRank
  combined: Float!
  vectorRank: Int
  keywordRank: Int
  vector: Float!
  # Keyword may be null because we first do a match using a GIN index
  keyword: Float
}
