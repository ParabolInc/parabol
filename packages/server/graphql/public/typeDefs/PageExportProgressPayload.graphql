"""
A transient live-progress event for a running page export, published on the
notification subscription after each per-page state change (no Notification row is written)
"""
type PageExportProgressPayload {
  """
  The export record with its updated per-page states
  """
  pageExport: PageExport!
}
