{
  schema: {
    files: "build/schema.graphql"
  },
  query: {
    files: [
      {
        match: 'src/**/*.js',
        parser: [ 'EmbeddedQueryParser', { startTag: 'graphql`', endTag: '`' } ],
        validate: {
          extends: 'gql-rules-query-relay',
          rules: {
            // disable directive validation
            KnownDirectives: 'off'
          },
        }
      },
    ]
  }
}
